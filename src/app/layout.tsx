import './globals.css'

export const metadata = {
  title: 'Spotify using Tailwind',
  description: 'Generated by create next app and styled using tailwind',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
